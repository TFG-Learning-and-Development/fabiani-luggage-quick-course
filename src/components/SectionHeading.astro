---
interface Props { title: string; introduction?: string; id: string }
const { title, introduction, id } = Astro.props;
---
<div class="section-heading">
  <h2 id={id}>{title}</h2>
  {introduction && <p>{introduction}</p>}
</div>
